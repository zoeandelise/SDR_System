# 紧急修复 - 数据显示问题

**问题**：API返回null，前端显示0  
**状态**：✅ 代码已修复，需要重启

---

## 🔍 API响应分析

### 当前API返回（有问题）
```json
"todayNutrition": {
    "totalCalories": null,  ← null！
    "totalProtein": null,
    "totalFat": null,
    "totalCarbohydrate": null
}

"todayRecords": [
    { "totalCalories": 630.00, "notes": "杏仁" },
    { "totalCalories": 840.00, "notes": "黄豆" },
    { "totalCalories": 839.00, "notes": "黑豆" },
    { "totalCalories": 840.00, "notes": "奶酪" },
    { "totalCalories": 840.00, "notes": "黄豆" },
    { "totalCalories": 840.00, "notes": "奶酪" }
]
```

**问题**：
- todayRecords有6条记录（数据库正常）
- 但todayNutrition全部null（计算失败）

---

## ✅ 修复方案

### 新增方法：直接SQL汇总
```java
private DietRecord calculateTodayNutrition(Long userId, Date today) {
    String sql = "SELECT " +
                "COALESCE(SUM(total_calories), 0) AS totalCalories, " +
                "COALESCE(SUM(total_protein), 0) AS totalProtein, " +
                "COALESCE(SUM(total_fat), 0) AS totalFat, " +
                "COALESCE(SUM(total_carbohydrate), 0) AS totalCarbohydrate " +
                "FROM diet_record " +
                "WHERE user_id = ? AND record_date = CURDATE()";
    
    // 直接查询汇总，不依赖Service层
    List<Map<String, Object>> results = jdbcTemplate.queryForList(sql, userId);
    
    // 解析结果并设置到DietRecord
    ...
}
```

### 调用新方法
```java
// 不再调用Service层的selectNutritionSummaryByUserIdAndDateRange
DietRecord todayNutrition = calculateTodayNutrition(userId, today);
```

---

## 🎯 预期结果

### 修复后API应该返回
```json
"todayNutrition": {
    "totalCalories": 4829.00,  ← 真实合计！
    "totalProtein": 404.40,
    "totalFat": 273.50,
    "totalCarbohydrate": 169.00
}
```

**前端显示**：
```
今日卡路里：4829 kcal
蛋白质：404.4g
碳水：169g
脂肪：273.5g
```

---

## 🚀 立即执行

### 第1步：停止旧后端
```batch
taskkill /F /IM java.exe
```

### 第2步：启动新后端
```batch
cd E:\study\BISHE\SDR_System\SDR_System-admin
mvn spring-boot:run
```

**等待30秒启动成功**

### 第3步：刷新前端
```
访问：http://localhost:3000/
按：Ctrl + Shift + R（强制刷新）
```

**应该看到**：
```
今日卡路里：4829 kcal（6条记录的真实合计）
蛋白质：404.4g
...
```

---

## ✅ 修复完成

**代码**：✅ 已修复  
**编译**：✅ SUCCESS  
**打包**：✅ SUCCESS  

**需要**：**立即重启后端！** ⚠️  

**重启后数据就正常了！** 🚀

