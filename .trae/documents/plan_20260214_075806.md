## 修复两个显示问题

### 问题1：碳水无法正常显示

**修改文件**：`SDR_System-admin/src/main/java/.../UserDietApiController.java`

* 第249行：将 `totalCarb` 改为 `totalCarbohydrate`

### 问题2：浮点数精度问题

**修改文件**：`sdr-user-frontend/src/pages/SmartRecommendationPage.tsx`

* 第131-134行：对累加结果进行四舍五入处理

* 第318-325行：`NutritionOverviewCard` 组件中格式化显示数值

### 具体修改

1. **后端** (UserDietApiController.java:249)

```java
// 修改前
plan.put("totalCarb", Math.round(totalCarb * 10) / 10.0);
// 修改后
plan.put("totalCarbohydrate", Math.round(totalCarb * 10) / 10.0);
```

1. **前端** (SmartRecommendationPage.tsx:131-134)

```typescript
// 修改前
updatedPlan.totalCalories = allFoods.reduce((sum, f) => sum + (f.calories || 0), 0);
updatedPlan.totalProtein = allFoods.reduce((sum, f) => sum + (f.protein || 0), 0);
updatedPlan.totalCarbohydrate = allFoods.reduce((sum, f) => sum + (f.carbohydrate || 0), 0);
updatedPlan.totalFat = allFoods.reduce((sum, f) => sum + (f.fat || 0), 0);

// 修改后 - 添加四舍五入
updatedPlan.totalCalories = Math.round(allFoods.reduce((sum, f) => sum + (f.calories || 0), 0));
updatedPlan.totalProtein = Math.round(allFoods.reduce((sum, f) => sum + (f.protein || 0), 0) * 10) / 10;
updatedPlan.totalCarbohydrate = Math.round(allFoods.reduce((sum, f) => sum + (f.carbohydrate || 0), 0) * 10) / 10;
updatedPlan.totalFat = Math.round(allFoods.reduce((sum, f) => sum + (f.fat || 0), 0) * 10) / 10;
```

1. **前端显示组件** (NutritionOverviewCard)

```typescript
// 添加数值格式化，确保显示整数或一位小数
<div className={`text-2xl font-bold ${color}`}>
  {typeof value === 'number' ? Math.round(value * 10) / 10 : value}
</div>
```

